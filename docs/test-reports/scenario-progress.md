# Progression Phase 2 — scenario-director

Suivi d'exécution des 7 scénarios d'intégration. Mis à jour après chaque scénario.

- [x] 01-candidature-validation-matching : ÉCHEC — `reviewTeacherApplication` renvoie 500 (`Champ 'levels' inconnu` dans le SELECT de `server/index.js` ~L2943). La candidature est passée `approved` par l'UPDATE préalable mais le compte enseignant n'est jamais créé → absent du matching admin ET conseiller. Bug applicatif bloquant confirmé et reproductible.
- [x] 02-demande-bilan-matching-staff-uniquement : PARTIEL — `confirmAssignment` (staff) fonctionne : liaison `student_teacher` créée, demande passée `assigné`, propagation OK vers Élève/Mes Professeurs et Enseignant/Mes Apprenants. Élève sans capacité de matching (conforme). BLOCAGE : l'automation `PATCH /requests/:id → 'en traitement'` échoue (`SELECT users.geo_location_id` — colonne inexistante, server/index.js ~L2042) → aucune assignation créée depuis une demande réelle. Écart secondaire : Parent/Équipe Pédagogique (Team.tsx) est basé sur les séances, pas sur `student_teacher` → enseignant non visible tant qu'aucune séance. Note sécurité : `PATCH /api/assignments/:id` non authentifié.
- [x] 03-seance-devoir-correction-suivi-parent : OK — chaîne complète verte : createSession → check-in/out → rapport → devoir lié (sessionId) → dépôt élève (upload PDF, statut 'rendu') → correction ('corrigé' + feedback) → visible côté Parent/Devoirs (feedback + fichier + indicateur séance). Écart documenté : Parent/Progression (progress-report) est statique (assiduité=95 figée, notes issues des seuls quiz) — la correction de devoir n'y est pas reflétée.
- [x] 04-facturation : OK — generateManualInvoices (admin) génère 1 facture/famille éligible, montant correct (2h défaut × 8000 = 16000 FCFA), statut 'pending', visible côté Parent/Factures avec description 'Cours de soutien'. Idempotence vérifiée (2e run = 0 dup). Périmètre vérifié : non-admin → 403.
- [x] 05-messagerie-croisee : OK — 3 paires (teacher↔parent, advisor↔parent, student↔teacher) : envoi (201), badge non-lu incrémenté (unread-count), marquage lu → badge décrémenté, upload + envoi de pièce jointe accessible côté destinataire. Toutes vertes.
- [x] 06-geographie-matching : OK — suggestion de quartier (POST /geo/suggest) visible dans /geo/pending et ABSENTE des listes actives avant validation ; validateGeoLocation (PATCH /geo/:id action=validate) → zone 'active' et disponible dans les listes déroulantes ; matching ultérieur : enseignant de la zone marqué « Zone ✓ » (locationMatch) avec score de proximité prépondérant (passe devant un enseignant mieux noté mais hors zone) et trié en tête.
- [x] 07-double-role-tutor-teacher : OK — équivalence stricte /teacher/* vs /tutor/enseignant/* (mêmes codes de retour createSession/homework/message/course pour l'enseignant standard et le double rôle tutor+secondary_role teacher) ; scope strict par userId (aucune élévation : élèves/cours d'un autre enseignant invisibles) ; espace Tuteur natif cloisonné (aucune candidature/évaluation créée par les actions enseignant). Drift consigné : POST/GET /courses cassés (colonnes mode/price/duration absentes) — identique pour les deux rôles, donc équivalence préservée.

## Bilan suite complète
35 tests d'intégration, 31 verts / 4 échecs (échecs = bugs documentés : scénario 01 x3 cascade `levels`, scénario 02 x1 automation `users.geo_location_id`). Aucune pollution croisée entre suites (cleanup ciblé par marqueur `it-*` / `[IT]`).
